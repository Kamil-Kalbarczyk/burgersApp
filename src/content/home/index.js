import { PageWrapper } from "../../common/page-wrapper";
import { Typography } from "@mui/material";

export const Home = () => {
  return (
    <PageWrapper title="Główna strona!">
      <Typography variant="subtitle1">Welcome to our App</Typography>
    </PageWrapper>
  );
};
