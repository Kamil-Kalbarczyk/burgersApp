import { PageWrapper } from "../../common/page-wrapper";
import { Typography } from "@mui/material";

export const Menu = () => {
    return <PageWrapper title="Menu">
        <Typography>Menu- Heloł!</Typography>
    </PageWrapper>
}